export default class Category {
  constructor(category) {
    this.id = category.id;
    this.name = category.name;
    this.image = category.image;
    this.description = category.description;
    this.lab = category.lab;
  }
}
